docker build -t gateway-web-backend .

docker run -it --name gateway-web-backend  \
-p 8080:8080 \
--net localNet --ip 192.168.0.5 gateway-web-backend /bin/bash

/*
 gateway-web-backend运行语句
 application.yml配置文件也需要进行更改
*/
docker run -d --name gateway-web-backend \
  -p 8080:8080 \
  --net localNet --ip 192.168.0.5 \
  -v /home/pcl/gateway/monitor/file_buffer:/app/file_buffer \
  gateway-web-backend
